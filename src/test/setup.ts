/**
 * Global test setup.
 *
 * `@testing-library/jest-dom` adds the DOM matchers (toBeInTheDocument, etc).
 * Cleanup after each test is automatic in Testing Library v16 when `globals`
 * is enabled, so it is not repeated here.
 */
import '@testing-library/jest-dom/vitest';
